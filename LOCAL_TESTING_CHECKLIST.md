## 🧪 قائمة اختبار البوت المحلي

اتبع هذه الخطوات لاختبار كل حاجة في البوت بعد تشغيله محلياً.

---

## ✅ قبل الاختبار

- [ ] البيئة الافتراضية مفعلة: `(venv)` يظهر في الـ Terminal
- [ ] البوت يعمل: ترى `✅ Waleed Zone Bot is running...`
- [ ] لديك معرف Telegram شخصي
- [ ] أنت في قائمة `ADMIN_IDS` إذا أردت اختبار ميزات الإدارة

---

## 1. اختبر القائمة الرئيسية 📋

```
على Telegram:
┌─────────────────────────┐
│ أرسل: /start            │
└─────────────────────────┘
```

**يجب أن ترى**:
- [ ] رسالة ترحيب "أهلاً وسهلاً بك"
- [ ] أزرار رئيسية:
  - [ ] 🔍 بحث
  - [ ] 📱 أحدث التطبيقات
  - [ ] 🏷️ التصنيفات
  - [ ] ★ المفضلة
  - [ ] (⚙️ الإدارة - إذا كنت أدمن)

---

## 2. اختبر المحادثة (Conversation) ✨

### أ) اختبر البحث

```
اضغط: 🔍 بحث
اكتب: "whatsapp" أو أي اسم تطبيق
```

**النتائج المتوقعة**:
- [ ] إذا كان التطبيق موجوداً: عرض التفاصيل
- [ ] إذا لم يكن: رسالة "لم نجد..."

### ب) اختبر المفضلة

```
اضغط: ★ المفضلة
```

**النتائج المتوقعة**:
- [ ] إذا كان عندك مفضلات: عرض القائمة
- [ ] إذا لم تكن: "لا توجد مفضلات بعد"

### ج) اختبر الفئات

```
اضغط: 🏷️ التصنيفات
اختر: (مثل "الإنتاجية" أو "الألعاب")
```

**النتائج المتوقعة**:
- [ ] عرض التطبيقات بهذه الفئة
- [ ] عدد التطبيقات (إن وجدت)

---

## 3. اختبر عرض التطبيق 📱

اضغط على أي تطبيق من النتائج.

**يجب أن ترى**:
- [ ] صورة التطبيق (إن وجدت)
- [ ] الاسم والإصدار
- [ ] الوصف الكامل
- [ ] عدد المحبين والمشاهدات
- [ ] أزرار:
  - [ ] ⬇️ تحميل
  - [ ] ❤️ حفظ كمفضل
  - [ ] 👈 رجوع

**اختبر الأزرار**:

```
اضغط: ⬇️ تحميل
```
- [ ] يجب أن يعطيك زر للتحميل من الخادم

```
اضغط: ❤️ حفظ كمفضل
```
- [ ] يجب أن يضيفه للمفضلة ✓

```
اضغط: 👈 رجوع
```
- [ ] يجب أن تعود لقائمة النتائج

---

## 4. اختبر الإدارة (إذا كنت أدمن) ⚙️

```
عند الضغط على /start:
اضغط: ⚙️ الإدارة
```

**يجب أن ترى قائمة الإدارة**:
- [ ] ➕ إضافة تطبيق
- [ ] 📋 إدارة التطبيقات
- [ ] 📬 الطلبات
- [ ] 📊 الإحصائيات
- [ ] (خيارات أخرى)

### أ) اختبر إضافة تطبيق

```
اضغط: ➕ إضافة تطبيق
```

**سيسأل**:

| السؤال | الرد المتوقع |
|--------|---------|
| الاسم؟ | WhatsApp التطبيق الجديد |
| الوصف؟ | تطبيق للتواصل |
| الفئة؟ | الاتصالات (اختر من قائمة) |
| المنصة؟ | Android اختر |
| الملف أو الرابط؟ | ارفع ملف APK أو رابط |

**بعد الانتهاء**:
- [ ] يعرض معاينة (Preview)
- [ ] أزرار:
  - [ ] ✅ نشر
  - [ ] ❌ إلغاء
  - [ ] ✏️ تعديل

### ب) اختبر إدارة التطبيقات

```
اضغط: 📋 إدارة التطبيقات
```

**يجب أن ترى**:
- [ ] قائمة التطبيقات (إن كانت هناك)
- [ ] لكل تطبيق: أزرار تعديل وحذف

```
اضغط على تطبيق ثم:
```
- [ ] 📝 تعديل الاسم
- [ ] ✏️ تعديل الوصف
- [ ] 🗑️ حذف
- [ ] 👁️ معاينة

---

## 5. اختبر الطلبات 📬

### من الأدمن:

```
اضغط: 📬 الطلبات
```

**يجب أن ترى**:
- [ ] قائمة الطلبات المعلقة (إن كانت)
- [ ] لكل طلب:
  - [ ] اسم المستخدم
  - [ ] اسم التطبيق المطلوب
  - [ ] تاريخ الطلب

### من المستخدم العادي:

أرجع للقائمة الرئيسية:

```
اضغط: 📬 طلب تطبيق
```

**سيسأل**:
- [ ] اسم التطبيق المطلوب؟
- [ ] وصف أو ملاحظات؟

بعد الإرسال:
- [ ] [ ] رسالة "تم استقبال طلبك ✓"
- [ ] الطلب يظهر عند الأدمن

---

## 6. اختبر الإحصائيات 📊 (أدمن فقط)

```
اضغط: 📊 الإحصائيات
```

**يجب أن ترى**:
- [ ] عدد المستخدمين
- [ ] عدد التطبيقات
- [ ] عدد التحميلات الإجمالي
- [ ] أحدث تطبيق تم إضافته

---

## 7. قائمة التصحيح 🐛

### المشكلة: الأزرار لا تستجيب

```
الأعراض:
- اضغط الزر لكن لا يحدث شيء
- القائمة تبقى نفسها
```

**الحل**:
1. افحص سجلات Terminal (هل فيه خطأ؟)
2. أعد تشغيل البوت: `Ctrl+C` ثم `python main.py`
3. الق رسالة `/start` مرة أخرى

### المشكلة: Database Error

```
الأعراض:
❌ sqlalchemy.exc.OperationalError
```

**الحل**:
```bash
# 1. اوقف البوت (Ctrl+C)

# 2. حذف قاعدة البيانات
rm bot.db  # Linux/Mac
del bot.db  # Windows PowerShell

# 3. شغّل البوت مرة أخرى
python main.py
```

### المشكلة: Timeout في الملفات الكبيرة

```
الأعراض:
⏱️ Timeout waiting for file download
```

**الحل**:
- الملف كبير جداً (> 2GB)
- استخدم رابط مباشر بدلاً من الملف
- أو عدّل `MAX_UPLOAD_BYTES` في `.env`

### المشكلة: بدون صور (صور فارغة)

```
الأعراض:
- التطبيقات بدون صورة
- أيقونة فارغة
```

**الحل**:
1. تأكد من وجود `IMGBB_API_KEY` في `.env`
2. شغّل السكربت: `python scripts/migrate_images.py`
3. سيرفع الصور تلقائياً

---

## 8. فحص قاعدة البيانات المحلية 🔍

بدون إيقاف البوت، افتح Terminal جديد:

### عرض كل البيانات

```bash
# تشغيل sqlite3
sqlite3 bot.db

# في sqlite3 prompt:
.tables                                    # اعرض الجداول

SELECT * FROM applications;                # اعرض التطبيقات
SELECT * FROM users;                       # اعرض المستخدمين
SELECT * FROM favorites;                   # اعرض المفضلة
SELECT * FROM requests;                    # اعرض الطلبات

.exit                                     # اخرج
```

### بحث محدد

```bash
sqlite3 bot.db "SELECT name, version FROM applications LIMIT 5;"

sqlite3 bot.db "SELECT * FROM users WHERE telegram_id = 123456789;"
```

---

## 9. المدة الزمنية للاختبار ⏱️

| المهمة | الوقت |
|--------|------|
| قائمة البحث | 2 دقيقة |
| إضافة تطبيق | 5 دقائق |
| إدارة التطبيقات | 3 دقائق |
| الطلبات | 2 دقيقة |
| الإحصائيات | 1 دقيقة |
| **الإجمالي** | **~13 دقيقة** |

---

## 10. ملخص الاختبار ✓

بعد انتهاء الاختبار، ضع علامة أمام كل عنصر:

- [ ] 1️⃣ القائمة الرئيسية تعمل
- [ ] 2️⃣ البحث يعمل
- [ ] 3️⃣ عرض التطبيق يعمل
- [ ] 4️⃣ المفضلة تعمل
- [ ] 5️⃣ الأدمن قادر على إضافة تطبيقات
- [ ] 6️⃣ الطلبات تُسجل
- [ ] 7️⃣ قاعدة البيانات تحفظ البيانات
- [ ] 8️⃣ لا توجد أخطاء في Logs

### إذا كل شيء ✅:
```
البوت جاهز للنشر على Railway! 🚀
```

### إذا في مشاكل ❌:
1. افحص السجلات بعناية
2. اقرأ رسالة الخطأ المعروضة
3. رجع إلى `LOCAL_DEVELOPMENT.md` 📖
4. إذا الشيء مستمر، ابحث عن الخطأ Google

---

**تذكر**: الاختبار المحلي يوفر لك الوقت والمشاكل قبل النشر على الإنتاج! ✨

